# Metro Bot - Бот для сообщений о составах метро

Профессионально переписанный Telegram-бот для сообщений об уникальных составах метро Санкт-Петербурга.

## 🌟 Особенности

### Надёжность
- ✅ SQLite база данных (вместо текстовых файлов)
- ✅ Thread-safe операции с данными
- ✅ Автоматическое восстановление после ошибок
- ✅ Retry механизм для API запросов
- ✅ Валидация всех пользовательских данных
- ✅ Защита от HTML injection
- ✅ Rate limiting для рассылок

### Функциональность
- 📨 Создание отчётов о составах
- 👤 Система банов пользователей
- 📢 Массовая рассылка (гонг)
- 🚆 Управление списком составов
- 📊 Статистика бота
- 🗂 Автоочистка старых состояний

### Производительность
- ⚡ Многопоточная обработка
- 🔄 Асинхронные операции
- 📝 Подробное логирование
- 🧹 Автоматическая очистка памяти

## 📦 Установка

### 1. Установить Python 3.8+

```bash
python3 --version  # Проверка версии
```

### 2. Клонировать репозиторий

```bash
cd /path/to/your/folder
# Распакуйте архив upbot_refactored
```

### 3. Установить зависимости

```bash
cd upbot_refactored
pip install -r requirements.txt
```

### 4. Настроить конфигурацию (опционально)

Отредактируйте `config.py` если нужно:
- Изменить токен бота (или используйте переменную окружения)
- Изменить ID канала
- Добавить/удалить админов

**Рекомендуется использовать переменные окружения:**

```bash
export BOT_TOKEN="ваш_токен"
export CHANNEL_ID="-ваш_channel_id"
```

### 5. Запустить бота

```bash
python3 main.py
```

Или в фоновом режиме:

```bash
nohup python3 main.py > /dev/null 2>&1 &
```

## 📖 Использование

### Для пользователей

1. Запустите бота: `/start`
2. Нажмите кнопку "📨 Сообщить о вагоне"
3. Следуйте инструкциям бота

### Для администраторов

**Управление банами:**
```
/ban <user_id> [причина]  — Забанить пользователя
/unban <user_id>          — Разбанить пользователя
/banlist                  — Список забаненных
```

**Рассылка:**
```
/gong  — Начать массовую рассылку
```

**Управление составами:**
```
/trains      — Показать список составов
/edittrains  — Редактировать список
```

**Статистика:**
```
/stats  — Статистика бота
/help   — Список команд
```

## 🗂 Структура проекта

```
upbot_refactored/
│
├── main.py              # Главный файл запуска
├── config.py            # Конфигурация
├── database.py          # Работа с БД
├── state_manager.py     # Управление состояниями
├── bot.py               # Обработчики отчетов
├── utils.py             # Вспомогательные функции
│
├── admin/
│   ├── ban.py          # Система банов
│   ├── gong.py         # Массовая рассылка
│   └── trains.py       # Управление составами
│
├── requirements.txt     # Зависимости
├── README.md           # Эта инструкция
│
└── (создаются автоматически)
    ├── bot_data.db     # База данных SQLite
    └── bot.log         # Лог файл
```

## 🔧 Технические детали

### База данных

Бот использует SQLite с следующими таблицами:

- `users` — Все пользователи
- `banned_users` — Забаненные пользователи
- `user_states` — Состояния диалогов
- `trains` — Список составов

### Логирование

Все события записываются в `bot.log`:
- INFO — Обычные операции
- WARNING — Предупреждения
- ERROR — Ошибки с traceback
- CRITICAL — Критические ошибки

### Безопасность

- ✅ HTML экранирование всех пользовательских данных
- ✅ Валидация длины текста
- ✅ Проверка форматов (маршруты, команды)
- ✅ Rate limiting для рассылок (20 сообщений/сек)
- ✅ Thread-safe операции с БД

### Обработка ошибок

- Автоматические retry (до 3 попыток) для API запросов
- Graceful shutdown при SIGINT/SIGTERM
- Автоматический перезапуск polling при ошибках
- Максимум 10 попыток перезапуска с увеличивающейся задержкой

## 🐛 Решение проблем

### Бот не запускается

1. Проверьте токен в `config.py`
2. Проверьте установку зависимостей: `pip list | grep pyTelegram`
3. Проверьте логи в `bot.log`

### Ошибки базы данных

Удалите файл `bot_data.db` — он пересоздастся автоматически:
```bash
rm bot_data.db
python3 main.py
```

### Бот не отвечает

1. Проверьте, запущен ли процесс: `ps aux | grep python`
2. Проверьте логи: `tail -f bot.log`
3. Перезапустите бота

### Рассылка не работает

- Убедитесь, что есть пользователи: `/stats`
- Проверьте rate limit (20 сообщений/сек с паузой 1 сек)
- Проверьте логи на ошибки Telegram API

## 📊 Мониторинг

Просмотр логов в реальном времени:
```bash
tail -f bot.log
```

Проверка размера базы данных:
```bash
ls -lh bot_data.db
```

Статистика бота (только для админов):
```
/stats
```

## 🔄 Обновление

1. Остановите бота (Ctrl+C или `kill <pid>`)
2. Сделайте backup базы данных:
   ```bash
   cp bot_data.db bot_data.db.backup
   ```
3. Обновите код
4. Запустите бота: `python3 main.py`

## 📝 Логи важных событий

- ✅ Запуск/остановка бота
- ✅ Регистрация новых пользователей
- ✅ Создание отчётов
- ✅ Публикация в канал
- ✅ Баны/разбаны
- ✅ Рассылки
- ✅ Обновление составов
- ✅ Ошибки API
- ✅ Очистка старых состояний

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте `bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа к файлам
4. Проверьте токен и ID канала

## 📜 Лицензия

Все права защищены.
